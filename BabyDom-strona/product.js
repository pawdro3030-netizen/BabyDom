const money=n=>n.toLocaleString('pl-PL',{style:'currency',currency:'PLN'}),id=new URLSearchParams(location.search).get('id'),product=(window.BABYDOM_CATALOG||[]).find(p=>p.id===id),page=document.querySelector('#product-page');if(!product){page.innerHTML='<div class="missing"><h1>Nie znaleźliśmy tego produktu.</h1><a class="button button-primary" href="index.html#produkty">Wróć do katalogu</a></div>'}else{document.title=`${product.name} | BabyDom`;const images=product.images?.length?product.images:[product.image];page.innerHTML=`<nav class="breadcrumbs"><a href="index.html">Strona główna</a><span>/</span><a href="index.html#produkty">Produkty</a><span>/</span><b>${product.category}</b></nav><section class="product-detail"><div class="gallery"><div class="main-photo">${images[0]?`<img id="main-image" src="${images[0]}" alt="${product.name}">`:'<span>BabyDom</span>'}</div>${images.length>1?`<div class="thumbnails">${images.map((image,i)=>`<button class="thumb ${i===0?'active':''}" data-image="${image}"><img src="${image}" alt="Zdjęcie ${i+1}"></button>`).join('')}</div>`:''}</div><div class="detail-copy"><p class="category">${product.category}</p><h1>${product.name}</h1><p class="product-subcategory">${product.subcategory.replace(/^.*> /,'')}</p><p class="detail-price">${money(product.price)}</p><p class="availability ${product.stock?'':'out'}">${product.stock?`Dostępne: ${product.stock} szt.`:'Produkt chwilowo niedostępny'}</p><button class="button button-primary detail-add" ${product.stock?'':'disabled'}>Dodaj do koszyka <span>+</span></button><div class="delivery-note">✓ Paczkomat: 12,99 zł<br>✓ Kurier: 15,99 zł<br>✓ Darmowa dostawa od 149 zł<br>✓ Wysyłka we wtorki i piątki</div></div></section><section class="description"><p class="eyebrow">Opis produktu</p><h2>Warto wiedzieć</h2><p>${product.description||'Szczegóły produktu dostępne wkrótce.'}</p></section>`;document.querySelectorAll('.thumb').forEach(button=>button.onclick=()=>{document.querySelector('#main-image').src=button.dataset.image;document.querySelectorAll('.thumb').forEach(item=>item.classList.remove('active'));button.classList.add('active')});document.querySelector('.detail-add')?.addEventListener('click',()=>{let cart=JSON.parse(localStorage.getItem('babydom-cart')||'[]'),item=cart.find(x=>x.id===product.id);if(item)item.qty++;else cart.push({id:product.id,name:product.name,price:product.price,qty:1});localStorage.setItem('babydom-cart',JSON.stringify(cart));location.href='index.html#produkty'})}
